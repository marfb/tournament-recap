import styled from 'styled-components';
import {motion} from 'framer-motion';

const DetailLayoutContainer = styled(motion.div)`
	display: block;
	height: 100%;
	width: 100%;
`;

export default {DetailLayoutContainer};
